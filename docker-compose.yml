services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: arianne
      POSTGRES_USER: arianne
      POSTGRES_PASSWORD: arianne
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
